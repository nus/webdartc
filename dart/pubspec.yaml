name: webdartc
description: WebRTC library for Dart — RFC-compliant, I/O-isolated implementation.
version: 0.1.0

environment:
  sdk: '>=3.11.0 <4.0.0'

resolution: workspace

dependencies:
  crypto: ^3.0.0
  ffi: ^2.1.0

dev_dependencies:
  ffigen: ^19.0.0
  lints: ^5.0.0
  test: ^1.25.0
