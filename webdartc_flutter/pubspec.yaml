name: webdartc_flutter
description: Flutter integration for webdartc — rendering Widget, codecs, camera/mic input, speaker output.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.11.0 <4.0.0'
  flutter: '>=3.24.0'

dependencies:
  flutter:
    sdk: flutter
  webdartc:
    path: ../webdartc

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
