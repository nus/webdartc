name: 'Set up MSVC dev environment'
description: >-
  Source vcvarsall.bat for the given target architecture and export
  only the env vars vcvarsall actually changes to subsequent steps
  via $GITHUB_ENV. vswhere.exe locates the installed Visual Studio
  regardless of edition (Enterprise / Community / BuildTools).
inputs:
  arch:
    description: 'MSVC target architecture (x64, arm64, x86_amd64, etc.)'
    required: true
runs:
  using: composite
  steps:
    - shell: pwsh
      # Dumping the full `set` output re-emits pre-existing vars under
      # cmd.exe's upper-case keys, while the runner keeps the originals
      # in their mixed case — MSBuild then sees both `PROGRAMW6432` and
      # `ProgramW6432` and rejects the duplicate. Snapshot the env
      # before vcvarsall.bat and forward only the entries it changed.
      run: |
        $vsPath = & "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe" -latest -property installationPath
        $vcvars = "$vsPath\VC\Auxiliary\Build\vcvarsall.bat"

        $before = @{}
        foreach ($e in [Environment]::GetEnvironmentVariables('Process').GetEnumerator()) {
          $before[$e.Key.ToUpperInvariant()] = $e.Value
        }

        $changed = [System.Collections.Generic.List[string]]::new()
        foreach ($line in & cmd /c "`"$vcvars`" ${{ inputs.arch }} >NUL && set") {
          if ($line -match '^([^=]+)=(.*)$') {
            $key = $matches[1]
            $val = $matches[2]
            if ($before[$key.ToUpperInvariant()] -ne $val) {
              $changed.Add("$key=$val")
            }
          }
        }
        $changed | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8NoBOM
